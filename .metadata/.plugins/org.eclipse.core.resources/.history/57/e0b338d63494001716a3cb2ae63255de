object week6 {
class Poly(val terms: Map[Int, Double]) {
	def + (other: Poly): Poly = {
		val pairList = (List(this, other) flatMap (poly => List(poly.terms)) flatMap (map => map.toList))
		this
	}
}
}